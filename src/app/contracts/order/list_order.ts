export class ListOrder{
    orderCode: string;
    username: string;
    totalPrice: number;
    createdDate: Date;  
    completed: boolean;  
}

// 'orderCode', 'userName', 'totalPrice', 'createdDate'